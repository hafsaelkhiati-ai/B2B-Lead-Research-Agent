{
  "name": "b2b-lead-agent",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build && cp -r build ../backend/public"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
