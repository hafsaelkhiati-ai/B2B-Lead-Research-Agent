{
  "name": "b2b-lead-agent",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node server.js",
    "install-deps": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
